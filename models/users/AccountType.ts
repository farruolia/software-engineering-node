/**
 * @file Declares Account Type enum type representing user's account type in Tuiter
 */

enum AccountType {
    Personal = "PERSONAL",
    Academic = "ACADEMIC",
    Professional = "PROFESSIONAL"
}

export default AccountType;
 